<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AnimatedTab.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:c="clr-namespace:AnimatedTab.Controls"
    xmlns:icons="clr-namespace:AnimatedTab.Font"
    BackgroundColor="#FFFFFF">
    <Grid RowDefinitions="80*,20*">
        <Grid Background="#000000" />
        <Grid
            Grid.Row="1"
            BackgroundColor="#FFFFFF"
            ColumnDefinitions="25*,25*,25*,25*">
            <c:TabItem
                Grid.Column="0"
                CurrentText="{x:Static icons:IconFont.list}"
                NextText="Home" />
            <c:TabItem
                Grid.Column="1"
                CurrentText="{x:Static icons:IconFont.bookmark}"
                NextText="bookmark" />
            <c:TabItem
                Grid.Column="2"
                CurrentText="{x:Static icons:IconFont.search}"
                NextText="search" />
            <c:TabItem
                Grid.Column="3"
                CurrentText="aA"
                NextText="font" />
        </Grid>
    </Grid>
</ContentPage>